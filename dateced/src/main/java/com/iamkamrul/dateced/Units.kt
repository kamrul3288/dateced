package com.iamkamrul.dateced

enum class FromNowUnit{DEFAULT,SECOND,DAY,HOUR,MINUTES,MONTH,YEAR}
enum class FromNowLocalizeUnit{SECONDS,DAY,DAYS,HOUR,HOURS,MINUTE,MINUTES,MONTH,MONTHS,YEAR,YEARS}
enum class DateCedTimeZone{LOCAL, UTC, GMT}
enum class TimeDifferenceUnit{MILLISECOND, SECOND, HOUR, MINUTES, DAY}