package com.iamkamrul.dateced

/*
 * Platform-specific extensions are defined in their respective source sets:
 *
 *   androidMain / jvmMain  → DateCed.jvm.kt
 *     - toJavaZonedDateTime()
 *     - toJavaLocalDateTime()
 *     - DateCed.fromJavaZonedDateTime(...)
 *     - DateCed.fromJavaLocalDateTime(...)
 *     - formatJvm(pattern)
 *     - DateCed.parseJvm(...)
 *
 * This file is intentionally left empty. Add `expect` declarations here only when
 * a feature genuinely requires a different implementation per platform.
 */
